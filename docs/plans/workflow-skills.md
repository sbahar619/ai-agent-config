## Goal

Create two workflow skills — discovery-pipeline (research → plan → tasks) and implement-pipeline (worktree → branch → implement/review/fix/commit loop → squash) — and update the plan doc to reflect the revised phase 3.

## Plan

1. **Discovery pipeline skill** — research → plan → conditional tasks breakdown; produces artifacts for the next workflow
2. **Implement pipeline skill** — worktree setup → dev branch → per-task implement/review/fix/commit loop → squash to clean branch; explicitly permits git operations as part of its contract
3. **Update plan and AGENTS.md** — revise phase 3 in the plan doc to reflect the two workflow skills instead of a single orchestrate skill, and complete phase 4 (AGENTS.md update)
